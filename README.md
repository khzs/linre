# linre

## Manual steps

* get the "standard" debian iso (not the netinst one)
* apparently if you put a blank root password during install then sudo works out of the box


## Scripted steps

```
curl -L https://github.com/khzs/linre/raw/main/anchor.sh | bash
```
